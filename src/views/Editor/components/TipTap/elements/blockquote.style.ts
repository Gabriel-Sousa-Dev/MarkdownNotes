import { css } from "../../../../../../styled-system/css";

export const blockquoteStyle = css({
    marginTop: '1.5rem',
    marginBottom: '1.25rem',
    paddingLeft: '16px',
    borderLeft: '4px solid token(colors.gray.200)'
});